# Assigning variables
name <- "John"
age <- 40
is_R_fun <- TRUE

# Outputting variables
print(name)
print(age)

# Checking the data type using class()
class(name)      # Returns "character"
class(age)       # Returns "numeric"
class(is_R_fun)  # Returns "logical"